coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 1%
        informational: true
    patch:
      default:
        target: 60%
        informational: true
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
