# 任务交接清单（模板）

> 使用方式：**复制本文档全文**到新文件（如 Issue、Wiki、或 `docs/进度/handoffs/YYYY-MM-DD-模块-交接.md`），删除占位说明后逐项填写。涉密信息只写**获取方式**，不写密钥正文。

---

## 1. 交接元信息

| 字段 | 内容 |
|------|------|
| 交接日期 | YYYY-MM-DD |
| 交出方 | 姓名 / 账号 |
| 接收方 | 姓名 / 账号 |
| 覆盖周期 | 例：即日起至版本 vX.Y 发布 |
| 关联模块（`module_id`） | 与 `docs/进度/模块进度登记簿.yaml` 一致 |

---

## 2. 工作范围与目标

- **当前负责功能 / 服务**：（列表）
- **本迭代必须完成的里程碑**：（列表）
- **已明确不做的范围**：（避免接收方误扩 scope）

---

## 3. 代码与分支

| 项 | 说明 |
|------|------|
| 默认开发分支 | 例：`main` / `develop` |
| 进行中的分支 | 分支名 → 目的 → 预计合并日 |
| 未合并 PR | 链接 + 阻塞原因 |
| 关键目录 / 包 | 例：`backend/internal/connect/...` |

---

## 4. 文档与规范

- **必读设计**：（链接列表）
- **已知文档与实现偏差**：（章节 + 应以代码还是文档为准；若应以代码为准，是否已开文档修正任务）
- **接口变更习惯**：是否已跑 `make proto`、是否已更新 `docs/接口与数据流架构约定.md`

---

## 5. 环境与权限（不含秘密）

| 项 | 说明 |
|------|------|
| 本地运行方式 | docker compose / 裸跑 等 |
| 依赖服务 | DB、Redis、MT Gateway、策略服务等 |
| 密钥与配置位置 | 例：环境变量名、内部密码管理器条目名 |
| 监控 / 日志入口 | 若有 |

---

## 6. 风险、故障与联系人

| 类型 | 内容 |
|------|------|
| 已知坑 | |
| 近期事故 / 线上问题 | |
| 升级依赖时的注意点 | |
| 升级/审批联系人 | |

---

## 7. 进度登记更新义务

- [ ] 已在 `docs/进度/模块进度登记簿.yaml` 将 `owner` 更新为接收方（或双方约定的新 owner）  
- [ ] 已更新 `percent` / `status` / `summary` 与当前事实一致  
- [ ] 已在 `docs/文档索引与维护状态.md` 或相关专项中更新「需复核」项（若本次交接发现文档滞后）

---

## 8. 接收方确认

接收方签字（或评论确认）：__________ 日期：__________

确认已理解：**未列在清单中的假设不默认成立**；有歧义时优先查 **`docs/文档索引与维护状态.md`** 与接口纲领。
