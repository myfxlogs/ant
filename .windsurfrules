# Project "ant" — Shared AI Rules

## Deployment（最重要 — GLM 踩过的坑）
- **后端部署唯一方式**: `docker compose build backend && docker compose up -d backend`
- **禁止**: 宿主机 `go build` → `docker cp`（glibc vs Alpine musl，二进制不兼容）
- **禁止**: 在运行中容器里 `go build` 或 `apk add build-base`
- **迁移文件**: `git status backend/migrations/` — 未提交的 .up.sql 会随 Docker build 执行

## Reuse Preflight（动工前）
- 写任何新 file/function 前，先查 `bash scripts/cap.sh <动词>`
- 在 PR 里标注 `REUSE: <symbol>` 或 `NEW: 无现成能力`

## Prohibited
- ❌ REST endpoints（除 healthz/readyz/livez/metrics）
- ❌ WebSocket
- ❌ JSON 序列化/持久化（proto only）
- ❌ float64 in price calculations（用 decimal.Decimal）
- ❌ `//nolint`, `# noqa`, `// @ts-ignore`

## Tech Stack
- External API: ConnectRPC + SSE
- Internal: in-process 或 NATS JetStream
- MT access: mtapi gRPC（adapter/mt4/ 和 mt5/ 不共享代码）
- Build: multi-stage Docker（golang:alpine → alpine runtime）
- Binary name: `/app/alphaforge`（不是 alphaforge-backend / server）
