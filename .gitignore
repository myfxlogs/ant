# Dependencies & build
node_modules/
**/node_modules/
dist/
build/
coverage/
*.exe
*.test

# Go build output (local)
backend/bin/
backend/main
backend/server
backend/cmd/server/server
backend/migrate-ch
backend/ant
backend/ant-vault
backend/hdgen
backend/md-doctor
backend/slo-report
backend/*.test

# Temp / tool artifacts
.tmp_*
.reasonix/

# Proto generated code — committed to repo so CI doesn't need `make proto` before build.
# The proto-drift CI job regenerates and checks for dirty working tree.

# Proto toolchain binaries (installed by `make proto-tools`)
tools/proto-gen/bin/

# Runtime
*.pid
*.out
__pycache__/
*.py[cod]
.pytest_cache/
nginx/logs/
.mypy_cache/

# Secrets & local env
.env
.env.*
!.env.example
docker-compose.override.yml

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
.windsurf/
.cursor/
.trae/
.deepseek/

# Local backups (large, not source of truth)
bak260421/
_nonproduction_archive/
backups/

# Local experiments / upstream mirrors — not part of AlphaForge product repo
Emulator/
mtproto/
test/
reference/

# Local test reports / traces
frontend/playwright-report/
frontend/playwright-traces/
frontend/test-results/
test-results/
tmp/

# Local credentials
id_ed25519
# 运维 SSH 私钥目录（如 ./ssh/id_ed25519），切勿提交
ssh/
.claude/*
!.claude/skills/
!.claude/skills/**
bin/alphaforge
bin/
e2e/shots/*
backend/test_email.go
.venv/
# frontend vitest suite lives here (tracked) — un-ignore despite blanket test/
!frontend/src/test/
