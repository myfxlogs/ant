// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: strategy_generation.proto

package antv1connect

import (
	v1 "anttrader/gen/proto/ant/v1"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StrategyGenerationServiceName is the fully-qualified name of the StrategyGenerationService
	// service.
	StrategyGenerationServiceName = "ant.v1.StrategyGenerationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StrategyGenerationServiceGenerateStrategyProcedure is the fully-qualified name of the
	// StrategyGenerationService's GenerateStrategy RPC.
	StrategyGenerationServiceGenerateStrategyProcedure = "/ant.v1.StrategyGenerationService/GenerateStrategy"
)

// StrategyGenerationServiceClient is a client for the ant.v1.StrategyGenerationService service.
type StrategyGenerationServiceClient interface {
	// GenerateStrategy orchestrates the full Phase 1 pipeline.
	// Returns a server-stream of chunks representing each pipeline phase.
	GenerateStrategy(context.Context, *connect.Request[v1.GenerateStrategyRequest]) (*connect.ServerStreamForClient[v1.GenerateStrategyChunk], error)
}

// NewStrategyGenerationServiceClient constructs a client for the ant.v1.StrategyGenerationService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStrategyGenerationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StrategyGenerationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	strategyGenerationServiceMethods := v1.File_strategy_generation_proto.Services().ByName("StrategyGenerationService").Methods()
	return &strategyGenerationServiceClient{
		generateStrategy: connect.NewClient[v1.GenerateStrategyRequest, v1.GenerateStrategyChunk](
			httpClient,
			baseURL+StrategyGenerationServiceGenerateStrategyProcedure,
			connect.WithSchema(strategyGenerationServiceMethods.ByName("GenerateStrategy")),
			connect.WithClientOptions(opts...),
		),
	}
}

// strategyGenerationServiceClient implements StrategyGenerationServiceClient.
type strategyGenerationServiceClient struct {
	generateStrategy *connect.Client[v1.GenerateStrategyRequest, v1.GenerateStrategyChunk]
}

// GenerateStrategy calls ant.v1.StrategyGenerationService.GenerateStrategy.
func (c *strategyGenerationServiceClient) GenerateStrategy(ctx context.Context, req *connect.Request[v1.GenerateStrategyRequest]) (*connect.ServerStreamForClient[v1.GenerateStrategyChunk], error) {
	return c.generateStrategy.CallServerStream(ctx, req)
}

// StrategyGenerationServiceHandler is an implementation of the ant.v1.StrategyGenerationService
// service.
type StrategyGenerationServiceHandler interface {
	// GenerateStrategy orchestrates the full Phase 1 pipeline.
	// Returns a server-stream of chunks representing each pipeline phase.
	GenerateStrategy(context.Context, *connect.Request[v1.GenerateStrategyRequest], *connect.ServerStream[v1.GenerateStrategyChunk]) error
}

// NewStrategyGenerationServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStrategyGenerationServiceHandler(svc StrategyGenerationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	strategyGenerationServiceMethods := v1.File_strategy_generation_proto.Services().ByName("StrategyGenerationService").Methods()
	strategyGenerationServiceGenerateStrategyHandler := connect.NewServerStreamHandler(
		StrategyGenerationServiceGenerateStrategyProcedure,
		svc.GenerateStrategy,
		connect.WithSchema(strategyGenerationServiceMethods.ByName("GenerateStrategy")),
		connect.WithHandlerOptions(opts...),
	)
	return "/ant.v1.StrategyGenerationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StrategyGenerationServiceGenerateStrategyProcedure:
			strategyGenerationServiceGenerateStrategyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStrategyGenerationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedStrategyGenerationServiceHandler struct{}

func (UnimplementedStrategyGenerationServiceHandler) GenerateStrategy(context.Context, *connect.Request[v1.GenerateStrategyRequest], *connect.ServerStream[v1.GenerateStrategyChunk]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("ant.v1.StrategyGenerationService.GenerateStrategy is not implemented"))
}
