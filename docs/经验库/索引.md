# 经验库索引

> 按 symptom / error / 技术栈 / 工具名检索。命中后读 `条目/<id>.md` 全文。
> 新条目入库：① 写 `条目/<id>.md` ② 更新本表 ③ 同 commit 提交。

| ID | 关键词 | 症状/场景 | 根因一句话 | 条目 |
|----|--------|----------|-----------|------|
| _（待积累）_ | — | — | — | — |

## 检索示例

```bash
# 按症状
grep "回测不开单" docs/经验库/索引.md
grep "指标全零" docs/经验库/索引.md

# 按报错
grep "bars are not chronologically ordered" docs/经验库/索引.md

# 按技术栈
grep "MQL2GO" docs/经验库/索引.md
grep "MT4" docs/经验库/索引.md
```

## 已有坑库指针

> 以下内容已在 `docs/pitfalls.md` 沉淀，经验库不重复，只放指针。

- **MQL2GO VM 静默失败** → docs/pitfalls.md §MQL2GO VM Pitfalls
- **Strategy Runner 实盘执行** → docs/pitfalls.md §Strategy Runner Rules
- **Schedule Engine 热循环** → docs/pitfalls.md §Strategy Schedule Engine Pitfalls
- **Backtest 状态断链** → docs/pitfalls.md §Backtest Status Management
- **Frontend Auth/Stream** → docs/pitfalls.md §Frontend Auth & Stream Error Pitfalls
- **Broker Snapshot/Stream** → docs/pitfalls.md §Broker Snapshot & Stream Pitfalls
- **PG 连接池** → docs/pitfalls.md §PG Connection Pool
- **运维手册** → docs/runbook/（mt-incidents.md / mql2go-known-pitfalls.md / stale-authoritative-snapshot.md 等）
