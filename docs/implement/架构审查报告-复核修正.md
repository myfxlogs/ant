# 架构审查报告 · 复核修正记录

> 对每项 claim 追查源码后的修正。

---

## 🔴 P0 项复核

| 原 # | Claim | 源码核实结果 | 修正 |
|:----:|-------|:-----------:|:----:|
| 1.1 | 明文密码绕过 secrets | ⚠️ 项目设计要求，非缺陷 | ❌ 已删除该条目 |
| 1.2 | SRE 端点绕过 AuthInterceptor | ❌ 判断不准确——`requireAdmin()` 使用相同的 JWT secret + `ValidateToken`，鉴权有效。但上下文未注入 `UserIDKey` | ⬇️ 降为 🟡 1.2，改为"鉴权路径不一致" |
| 1.3 | 双认证栈 | ✅ `service.AuthService` 零调用方，是死代码 | ⬇️ 降为 🟡 1.3，改为"死代码" |
| 1.4 | Handler 直连 Repository | ✅ 确认 `admin/` handlers 直接注入 `*repository.AdminRepository` | 保持 |
| 1.5 | Setter 注入反模式 | ✅ 确认 `account_handler.go:37-39` 有 `SetSearcher`/`SetPublisher` | 保持 |

## 🟡 P1 项复核

| # | Claim | 核实结果 | 修正 |
|:-:|-------|:--------:|:----:|
| 2.1 | sqlc + 手写 SQL 双轨 | ✅ 确认 `repository/db.go` 有 sqlc `Queries` 结构 + 18+ 手写 repo | 保持 |
| 2.2 | 全局 Clk 竞态 | ✅ 确认 8 个包各有 `var Clk`，并行测试不安全 | 保持 |
| 2.3 | jwt 全局单例 | ✅ 确认 `var jwtConfig *JWTConfig` 全局，`Init()` 只能调一次 | 保持 |
| 2.4 | CostEstimator 无 error 返回 | ✅ 确认 `Estimate()` 返回 `CostBreakdown`，无 error | 保持 |
| 2.5 | FillModel 双重滑点 | ❌ 判断不准确——随机滑点（执行噪声）和 SlippageBps（结构成本）概念不同。但关系未文档化 | 改为"关系不明确" |
| 2.6 | 重复 Sharpe | ✅ 确认两处实现：`backtest/metrics.go` 和 `ai/walkforward.go` | 保持 |
| 2.7 | execalgo 验证重复 | ✅ 确认 4 个 algo 各重复相同参数验证 | 保持 |
| 2.8 | OTel Shutdown 缺失 | ⚠️ `mdgateway/runner.go:60` 有 defer，但 main.go 创建的 tracer 路径未一致 defer | 保持 |
| 2.9 | platformAgg 可变全局 | ✅ 确认 `var platformAgg` 在闭包中引用、闭包外赋值 | 保持 |
| 2.10 | 内存限流器 | ✅ 确认注释写明 in-memory only | 保持 |

## 🟢 P3 代码质量项复核

| # | Claim | 核实结果 | 修正 |
|:-:|-------|:--------:|:----:|
| Commision max-of-two 隐式 | ❌ 代码注释已写明"whichever produces the higher cost" | ❌ 已删除该条目 |
| Shortfall float 累积 | ⚠️ 代码已有 `vol = remaining` 处理最后一笔，称"bug"不当 | 修正措辞 |

---

## 复核结论

- **原 17 项** → 核实后保留 **15 项**（删除 1 项明文密码、1 项 Commission 隐式）
- **P0 从 5 项降为 4 项**（SRE 降为 🟡、双认证栈降为 🟡、明文密码删除）
- **2 项需要措辞修正**（FillModel 双重滑点、Shortfall 累积误差）
- **0 项是误报**（所有保留的 claim 都有源码确认）
