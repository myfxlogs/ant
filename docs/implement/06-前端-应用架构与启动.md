# 06 · 前端应用架构与启动

**涉及源码**：
- `frontend/src/main.tsx`
- `frontend/src/App.tsx`
- `frontend/src/i18n/index.ts`

---

## 1. 应用启动（main.tsx）

```typescript
// 1. 先初始化 i18n（防止懒加载组件拿到非函数 `t`）
// 2. 懒加载 App 组件
// 3. React 18 createRoot

async function bootstrap() {
    if (!i18n.isInitialized) { await i18n.init(); }
    await Promise.resolve();
    const { default: App } = await import('./App');
    createRoot(document.getElementById('root')!).render(<App />);
}
bootstrap();
```

---

## 2. i18n 配置（i18n/index.ts）

### 2.1 语言支持

```typescript
export const SUPPORTED_LANGUAGES = ['zh-cn', 'zh-tw', 'en', 'ja', 'vi'] as const;
export type SupportedLanguage = (typeof SUPPORTED_LANGUAGES)[number];
export const LANGUAGE_STORAGE_KEY = 'anttrader_lang';
```

### 2.2 语言检测

```typescript
// 优先级：localStorage('anttrader_lang') → navigator.languages[0] → 'zh-cn'
export function normalizeLanguage(input): SupportedLanguage {
    // zh-Hans → 'zh-cn'
    // zh-Hant / zh-HK / zh-MO → 'zh-tw'
    // ja → 'ja', vi → 'vi'
    // 其他 → 'en'
}
```

### 2.3 资源动态加载

```typescript
// 每个语言的翻译资源独立打包（code-split）
const resourceCache = new Map<string, Record<string, unknown>>();

async function loadBundle(lng: string): Promise<Record<string, unknown>> {
    if (resourceCache.has(lng)) return resourceCache.get(lng)!;
    const mod = await import(`./resources/${lng}/index.ts`);
    resourceCache.set(lng, mod.default);
    return mod.default;
}
```

### 2.4 初始化

```typescript
i18n
    .use(LanguageDetector)
    .use(initReactI18next)
    .init({
        lng: initial,            // 首次语言
        fallbackLng: 'en',       // 回退语言
        interpolation: { escapeValue: false },
        detection: {
            order: ['localStorage', 'navigator'],
            caches: [],          // 不缓存检测结果
        },
        react: { useSuspense: false },
    });
```

### 2.5 语言切换

```typescript
export async function setLanguage(lng: SupportedLanguage) {
    const bundle = await loadBundle(normalized);
    if (!i18n.hasResourceBundle(normalized, 'translation')) {
        i18n.addResourceBundle(normalized, 'translation', bundle, true, true);
    }
    i18n.changeLanguage(normalized);
    localStorage.setItem(LANGUAGE_STORAGE_KEY, normalized);
}
```

### 2.6 BigInt 兼容

```typescript
// Protobuf int64 可能产生 BigInt，而 Intl.PluralRules.select() 会报错
// Polyfill：将 BigInt 转为 Number
if (Intl.PluralRules) {
    const _select = Intl.PluralRules.prototype.select;
    Intl.PluralRules.prototype.select = function (count: number | bigint) {
        if (typeof count === 'bigint') count = Number(count);
        return _select.call(this, count);
    };
}
```

---

## 3. App 组件

### 3.1 语言监听

```typescript
function App() {
    const [lang, setLang] = useState<SupportedLanguage>(normalizeLanguage(i18n.language));
    const [antdLocale, setAntdLocale] = useState(null);

    useEffect(() => {
        const handler = (lng: string) => setLang(normalizeLanguage(lng));
        i18n.on('languageChanged', handler);
        return () => { i18n.off('languageChanged', handler); };
    }, []);

    // 语言变更时动态加载 dayjs locale + antd locale
    useEffect(() => {
        const dl = dayjsLocaleMap[lang] || 'en';
        const ak = antdLocaleKeyMap[lang] || 'en_US';
        // lazy-load dayjs locale
        // lazy-load antd locale（模块级缓存 antdLocaleCache）
    }, [lang]);
    // ...
}
```

### 3.2 Token 生命周期管理

```typescript
useEffect(() => {
    let cancelled = false;
    void (async () => {
        const { startTokenScheduler, ensureFreshToken } = await import('@/utils/tokenLifecycle');
        if (cancelled) return;
        await ensureFreshToken();       // 启动时预刷新
        startTokenScheduler();          // 定时后台刷新
    })();
    return () => { cancelled = true; };
}, []);
```

### 3.3 应用结构

```tsx
<ConfigProvider locale={antdLocale}>
    <QueryProvider>
        <BrowserRouter>
            <AppContent />
        </BrowserRouter>
    </QueryProvider>
</ConfigProvider>
```

### 3.4 AppContent 组件

```typescript
function AppContent() {
    const { _hasHydrated } = useAuthStore();

    if (!_hasHydrated) {
        // 等待 localStorage 中持久化的 auth 状态恢复
        return <Spin size="large" />;
    }

    return (
        <ConnectProvider>
            <Routes>
                {/* 公开路由：/login, /register, /forgot-password, /terms, /privacy */}
                {/* 私有路由（带 MainLayout）：/, /accounts/*, /trading, /market, /ai/*, /strategy/*, /logs */}
                {/* 管理员路由（带 AdminLayout）：/admin/* */}
            </Routes>
        </ConnectProvider>
    );
}
```

---

## 4. 路由架构

```
<BrowserRouter>
  ├─ /login, /register, /forgot-password      → PublicRoute（已登录则跳 /）
  ├─ /terms, /privacy                          → 公开页面
  ├─ /                                         → PrivateRoute > MainLayout
  │   ├─ index                                 → Dashboard
  │   ├─ accounts/:id                          → AccountDetail
  │   ├─ accounts/bind                         → BindAccount
  │   ├─ trading                               → Trading
  │   ├─ market                                → Market
  │   ├─ marketplace                           → Marketplace
  │   ├─ analytics                             → Summary
  │   ├─ ai/*                                  → AIAssistantLayout
  │   │   ├─ debate                            → DebatePageV2
  │   │   ├─ settings                          → SystemAI
  │   │   ├─ agents                            → AISettings
  │   │   └─ gate                              → GateProgressPage
  │   ├─ strategy/templates                    → StrategyTemplatePage
  │   ├─ strategy/experiments                  → StrategyExperimentPage
  │   ├─ strategy/market-regime                → MarketRegimePage
  │   ├─ strategy/assets                       → StrategyAssetPage
  │   ├─ strategy/schedules                    → StrategySchedulePage
  │   ├─ strategy/schedules/:id/logs           → StrategyScheduleLogsPage
  │   └─ logs                                  → LogManagement
  └─ /admin                                    → AdminRoute > AdminLayout
      ├─ index                                 → AdminDashboard
      ├─ users                                 → UserManagement
      ├─ accounts                              → AccountManagement
      ├─ trading                               → TradingMonitor
      ├─ logs                                  → OperationLogs
      ├─ config                                → SystemConfig
      ├─ jurisdiction                          → JurisdictionGate
      └─ sre/*                                 → SRELayout
          ├─ killswitch                        → KillSwitchPage
          ├─ breakers                          → BreakersPage
          └─ canary                            → CanaryPage
```

### 路由守卫

```typescript
function PrivateRoute({ children }) {
    const { isAuthenticated } = useAuthStore();
    return isAuthenticated ? children : <Navigate to="/login" />;
}

function PublicRoute({ children }) {
    const { isAuthenticated } = useAuthStore();
    return isAuthenticated ? <Navigate to="/" /> : children;
}

function AdminRoute({ children }) {
    const { isAuthenticated, user } = useAuthStore();
    if (!isAuthenticated) return <Navigate to="/login" />;
    if (!user?.permissions?.includes('admin:view')) return <Navigate to="/" />;
    return children;
}
```

### 页面懒加载

所有页面使用 `React.lazy()`：

```typescript
const Login = lazy(() => import('@/pages/auth/Login'));
const Dashboard = lazy(() => import('@/pages/dashboard/Dashboard'));
const Trading = lazy(() => import('@/pages/trading/Trading'));
// ... 共约 25 个页面
```
