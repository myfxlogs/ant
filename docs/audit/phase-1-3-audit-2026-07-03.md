# Phase 1-3 Implementation Audit Report

**日期**: 2026-07-03
**范围**: ADR-0024 Phase 1 (compile_py.go) + Phase 2 (盲区桥接) + Phase 3 (策略生成)
**方法**: 三路并行审计 — 编译管线 / Agent引擎 / 全局约束合规

---

## Constraint Compliance: ✅ PASS (9/9)

| 检查项 | 结果 |
|--------|------|
| JSON prohibition | PASS |
| REST prohibition | PASS |
| WebSocket prohibition | PASS |
| float64 in prices | PASS |
| Suppression comments | PASS |
| File size (hard limit) | PASS |
| Hardcoded secrets | PASS |
| Cross-scope changes | PASS |
| Function size (soft) | NOTE — 2 orchestrator functions exceed 50 lines |

---

## Defect Summary

### CRITICAL — 4 defects (1 bug + 3 unwired features)

| ID | Phase | Location | Defect |
|---|---|---|---|
| **C1** | P1 | `compile_py_stmt.go:170` | **`for i in range(N)` initializes `i=N` not `i=0` → loop executes zero times.** Silent wrong results. |
| **C2** | P2-3 | `memory.go` + `gateway.go` | **MemoryStore fully implemented but never wired in.** Gateway stubs return empty. Session startup does not inject memory into prompts. |
| **C3** | P2-3 | Nonexistent | **Zero hook integration.** ADR-0025 §8 lifecycle hooks (PreStrategySubmit, PostBacktest, etc.) not implemented. |
| **C4** | P2-3 | `generator.go:61-299` | **Generator.Generate() is 239 lines.** Single monolithic function encompassing planning, generation, compilation, backtest, analysis, retry, and streaming. |

### HIGH — 7 defects

| ID | Phase | Location | Defect |
|---|---|---|---|
| **H1** | P1 | `compile_py_subset.go:95` | **Type consistency not checked.** `validateTypeAnnotations` only checks annotations exist, never validates expression type matches declaration. `x: int = "hello"` passes. |
| **H2** | P1 | `compile_py_expr.go:171` | **`Decimal("0.1")` passes through as string literal.** Conversion to Decimal deferred to VM runtime, not documented. |
| **H3** | P1 | `compile_py_subset.go:236` | **Multiple inheritance silently accepted.** ADR D3 says "禁止", code ignores extra bases instead of rejecting. |
| **H4** | P2-3 | `bridge.go:105` | **Bridge does NOT retry on LLM API failure.** Only compile/backtest failures are retried. LLM transient error → immediate fail. |
| **H5** | P2-3 | `bridge.go:80` | **Retry count is 2, not 3.** `maxBridgeRetries=3`, loop starts at attempt=2 → 3 total attempts = 1 initial + 2 retries. ADR §5.4 says "含3次重试". |
| **H6** | P2-3 | `generator.go:317,356` vs others | **Inconsistent LLM timeouts.** Plan/profile generation uses 15s, all other LLM calls use 30s. No rationale. |
| **H7** | P2-3 | All agent files | **Session memory injection never reaches LLM.** `LoadSessionMemory`/`InjectIntoPrompt` exist in `memory.go` but zero agent components call them. |

### MEDIUM — 8 defects

| ID | Phase | Location | Defect |
|---|---|---|---|
| **M1** | P1 | `interp/ir.go:76` | `Version` field comment says "mql4" or "mql5" — missing "python" |
| **M2** | P1 | Multiple | Panic-based compile error handling. Functional but loses structured error info the ADR requires. |
| **M3** | P1 | `compile_py_compare.go:67` | `<>` operator dead code (removed in Python 3, tree-sitter-python never produces it) |
| **M4** | P1 | `compile_py_test.go` | Behavioral tests never assert arithmetic correctness — masked C1 |
| **M5** | P2-3 | `generator.go:112` | Plan mode path has no LLM cost accounting (cost ceiling inaccurate in plan-only mode) |
| **M6** | P2-3 | `helpers.go:127` `interpreter.go:145` `plan_prompt.go:56` | KEY:value line parser duplicated 3 times — should share a generic base |
| **M7** | P2-3 | `bridge.go:56-74` | Cache stores pre-parsed LLM output but re-parses on every hit — minor inefficiency |
| **M8** | P2-3 | `agent_gateway.proto:231` | `fingerprint` field comment says "JSON string" — proto comment only, no runtime violation |

### LOW — 12 observations

All file sizes within limits. No JSON/REST/float64/WebSocket violations. Compile.go has zero version-specific branching (correct). Prompts deduplicated via `prompts_shared.go`. Language dispatch correctly skips bridge for Python. Degradation path correctly calls `buildBridgeFailureReport`. All ADR D3 allowed/prohibited constructs handled (except H1-H3 above).

---

## Recommended Actions

### Must fix before Phase 4 (builds on these)

1. **C1** — Fix `range(N)` init value. Add behavioral assertions to test.
2. **C2** — Wire `MemoryStore` into `GatewayServer` and `Generator`. Inject session memory at generator startup.
3. **H4** — Add retry on LLM API failure in bridge (don't return on first error).
4. **H1** — Add compile-time type consistency checking OR document deliberate deferral.

### Should fix during Phase 4

5. **H5** — Clarify retry count: either change loop to `attempt := 1; attempt <= maxBridgeRetries` or change `maxBridgeRetries` to 4.
6. **H2** — Emit Decimal constructor in IR, or document VM runtime conversion dependency.
7. **H3** — Add multiple inheritance rejection in `compileClass`.
8. **C3** — Implement hook engine (`hook_engine.go` + `agent_hooks.proto`) as Phase 4 task.
9. **C4** — Refactor `Generator.Generate()` into phase-level methods.

### Nice to have

10. **M1-M8** — Stale comments, dead code, duplicated parsing, test coverage gaps
