<clickhouse>
    <!-- ant v2 ClickHouse runtime overrides (ADR-0002) -->
    <!-- max_memory_usage is a user-level setting; configured via SQL in chmigrate -->
    <max_connections>64</max_connections>
    <max_concurrent_queries>16</max_concurrent_queries>
</clickhouse>
