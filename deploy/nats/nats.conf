# ant v2 NATS configuration (single-node, JetStream enabled)
# spec/11 §8, §13.5

port: 4222
http_port: 8222

authorization {
  user: $NATS_USER
  password: $NATS_PASSWORD
}

jetstream {
  store_dir: /data/jetstream
  max_memory_store: 256MiB
  max_file_store: 15GiB
}
